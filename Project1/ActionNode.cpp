#include "ActionNode.hpp"

bool FunctionA::Execute()  {
//là on place le futur state pour la fonctionA
    //enemy.setCurrentState(enemy.PATROL);
    return true;
}
bool FunctionB::Execute()  {
    // là on place le futur state pour la fonctionB
    //enemy.setCurrentState(enemy.PROTECT);
    return true;
}